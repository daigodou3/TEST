# Vanilla + Flashlight (pass-through visuals; composite adds a flashlight cone)
# No GUI options; toggle flicker in shaders/settings.glsl

# Version: v2 (vanilla day/night brightness)

# Flashlight now only active when a normal torch is in the OFF-HAND (see item.properties)
